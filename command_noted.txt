run server 
py manage.py runserver

make DB migration 
python manage.py makemigrations ...

run DB migration 
python manage.py sqlmigrate ... 0001

API Shell 
python manage.py shell

Create admin 
python manage.py createsuperuser
(noted)
we need to tell the admin that "..." objects have an admin interface

apply changes to the database.
python manage.py migrate

#slug 
A short label for something, containing only letters, numbers, 
underscores or hyphens. They’re generally used in URLs
example:
https://www.djangoproject.com/weblog/2008/apr/12/spring/

#related_name: 
use for backward relationship in FK
class child:
    parent = .FK(parent,relation = "children")
class parent:
    ... 
child in parent.child_set.all() = child in parent.children

#manager 
class Person(models.Model):
    #...
    people = models.Manager()
Person.people.all() will provide a list of all Person objects.

#Modifying a manager’s initial QuerySet
#add both objects and specific_objects 
#Book.objects.all() will return all books in the database,
Book.specific_objects.all() will only return the ones written