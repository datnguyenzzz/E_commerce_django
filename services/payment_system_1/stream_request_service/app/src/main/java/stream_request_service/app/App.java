/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package stream_request_service.app;

import stream_request_service.list.LinkedList;

import static stream_request_service.utilities.StringUtils.join;
import static stream_request_service.utilities.StringUtils.split;
import static stream_request_service.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
